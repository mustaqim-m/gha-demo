name: Hello world
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build step
        run: echo "A demo build"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test step
        run: echo "A demo Test"
  Deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy step
        run: echo "A demo app deploy"
    
        
    
